Questions to Spencer/Ryan

1. AddPostTableViewController -> Dismiss doesn´t work

A: Because modal push has no navigation controller -> create one with embed in

2. AddPostTableViewController -> NavigationBar hides by modal push
A: see 1

2. Post -> convenience init in extension CKRecord

A: First designated initializer and then extra stuff


3. Difference between Dismiss in DVC and
   controller.navigationController?.popViewControllerAnimated(true) in SVC
A: see 1

4. set / get for computed properties vs. willset didset in normal propertys ?

A: computed properties has not didset / willset


5. SearchBarController -

   let searchResultsController = UISearchController(searchResultsController: SearchResultsTableViewController())

    vs.

   let searchResultsController = UISearchController(searchResultsController: nil)
